import LessText from "./components/LessText";

function App() {
  

  return (
   
      <div>
        <LessText text = {"Nothing is black-and-white, except for winning and losing, and maybe that's why people gravitate to that so much."} maxLength = {35}/>
        <LessText text = {"Learning how to rein in your thoughts and focus is going to help you to achieve your goals in life. Without focus, you can never achieve anything."} maxLength = {35}/>
      </div>
    
  );
}

export default App;
